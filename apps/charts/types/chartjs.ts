export interface LineProps {
    data: any;
    labelName: string;
    labelData: string;
};

export interface BarProps {
    data: any;
    xLabel: string;
    yLabel: string;
    maxValue: number
};

export interface PolarBarProps {
    data: any;
    label: string;
};
